import subprocess
import logging

logger = logging.getLogger('recon_ng_inst')

def instalar_recon_ng():
    logger.info("Instalando Recon-ng...")
    # Aquí se agregará la lógica de instalación específica de Recon-ng
    print("Recon-ng instalado correctamente.")
    logger.info("Recon-ng instalado correctamente.")

if __name__ == "__main__":
    instalar_recon_ng()
